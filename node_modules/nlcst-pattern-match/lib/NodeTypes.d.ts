import { Node } from "unist-types";
export interface TagNode extends Node {
    length?: number;
    isNegative?: boolean;
}
export declare const isPatternNode: (v: any) => v is PatternNode;
export interface PatternNode extends Node {
    type: "PatternNode";
    pattern: RegExp;
}
