/// <reference types="node" />
import { PortablePath } from '@yarnpkg/fslib';
import { RequestError, Response } from 'got';
import { URL } from 'url';
import { Configuration } from './Configuration';
export { RequestError } from 'got';
/**
 * Searches through networkSettings and returns the most specific match
 */
export declare function getNetworkSettings(target: string | URL, opts: {
    configuration: Configuration;
}): {
    caFilePath: PortablePath | null;
    enableNetwork: boolean | null;
    httpProxy: string | null;
    httpsProxy: string | null;
};
export declare type Body = ({
    [key: string]: any;
} | string | Buffer | null);
export declare enum Method {
    GET = "GET",
    PUT = "PUT",
    POST = "POST",
    DELETE = "DELETE"
}
export declare type Options = {
    configuration: Configuration;
    customErrorMessage?: (err: RequestError) => string | null;
    headers?: {
        [headerName: string]: string;
    };
    jsonRequest?: boolean;
    jsonResponse?: boolean;
    method?: Method;
};
export declare function request(target: string | URL, body: Body, { configuration, headers, jsonRequest, jsonResponse, method }: Omit<Options, 'customErrorMessage'>): Promise<Response<any>>;
export declare function get(target: string, { configuration, jsonResponse, ...rest }: Options): Promise<any>;
export declare function put(target: string, body: Body, { customErrorMessage, ...options }: Options): Promise<Buffer>;
export declare function post(target: string, body: Body, { customErrorMessage, ...options }: Options): Promise<Buffer>;
export declare function del(target: string, { customErrorMessage, ...options }: Options): Promise<Buffer>;
