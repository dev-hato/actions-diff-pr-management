import { RuleOption } from "../RuleOptions";
import { MODE } from "../mode";
import { Proofdict } from "@proofdict/tester";
/**
 * @param options
 * @param {string} mode
 * @returns {*}
 */
export declare const getDictionary: (options: RuleOption, mode: MODE) => Promise<Proofdict | undefined>;
