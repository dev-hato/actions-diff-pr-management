// MIT © 2018 azu
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRuleURL = exports.getDictJSONURL = void 0;
var url_join_1 = __importDefault(require("url-join"));
/**
 * @param options
 * @returns {string}
 */
function getDictJSONURL(options) {
    if (typeof options.dictURL === "object" &&
        typeof options.dictURL.jsonAPI === "string" &&
        typeof options.dictURL.ruleBase === "string") {
        return options.dictURL.jsonAPI;
    }
    if (typeof options.dictURL === "string") {
        return url_join_1.default(options.dictURL, "dictionary.json");
    }
    throw new Error("options.dictURL is undefined");
}
exports.getDictJSONURL = getDictJSONURL;
/**
 * @param options
 * @param rule
 * @returns {string|undefined}
 */
function getRuleURL(options, rule) {
    if (!rule) {
        return;
    }
    // http://custom.example.com/base#id
    if (typeof options.dictURL === "object" && typeof options.dictURL.ruleBase === "string") {
        return options.dictURL.ruleBase + "#" + encodeURIComponent(rule.id);
    }
    // http://example.com#id
    if (options.dictURL) {
        return options.dictURL + "#" + encodeURIComponent(rule.id);
    }
    return;
}
exports.getRuleURL = getRuleURL;
//# sourceMappingURL=proofdict-repo-util.js.map