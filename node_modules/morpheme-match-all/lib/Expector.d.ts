import { Token, ExpectedToken, MatchResult } from "morpheme-match";
export declare type ExpectedDictionary<T extends ExpectedToken> = {
    tokens: T[];
    message: string;
    expected?: string;
};
export declare class Expector<T extends ExpectedToken, Dictionary extends ExpectedDictionary<T>> {
    dict: Dictionary;
    private matcher;
    constructor(dict: Dictionary);
    /**
     * match and return results
     */
    match(actualToken: Token): MatchResult;
}
