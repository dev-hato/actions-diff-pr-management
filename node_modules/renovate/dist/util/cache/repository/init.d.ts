import type { RenovateConfig } from '../../../config/types';
/**
 * Extracted to separate file in order to avoid circular module dependencies.
 */
export declare function initRepoCache(config: RenovateConfig): Promise<void>;
