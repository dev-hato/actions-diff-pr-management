export interface MigratedData {
    content: string;
    filename: string;
}
interface Indent {
    amount: number;
    indent: string;
    type?: string;
}
export declare function applyPrettierFormatting(content: string, parser: string, indent: Indent): Promise<string>;
export declare class MigratedDataFactory {
    private static data;
    static getAsync(): Promise<MigratedData | null>;
    static reset(): void;
    private static build;
}
export {};
