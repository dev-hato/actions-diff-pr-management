import type { PackageFile } from '../../modules/manager/types';
import { BranchConfig, SelectAllConfig } from '../types';
export declare function readDashboardBody(config: SelectAllConfig): Promise<void>;
export declare function ensureDependencyDashboard(config: SelectAllConfig, allBranches: BranchConfig[], packageFiles?: Record<string, PackageFile[]>): Promise<void>;
