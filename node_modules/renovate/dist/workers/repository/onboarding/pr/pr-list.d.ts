import type { RenovateConfig } from '../../../../config/types';
import type { BranchConfig } from '../../../types';
export declare function getPrList(config: RenovateConfig, branches: BranchConfig[]): string;
